#!/usr/bin/env bash
# 瘦身后本地不维护官方 crates，全 workspace 测试移到 CI（check-upstream.yml 构建验证；
# 后续可在该 workflow 加 test job）。本地推送前如需验证：`nix build .#qingjianServer`。
# 跳过：git push --no-verify
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "提示：完整构建/测试由 CI 负责；本地验证用 nix build .#qingjianServer"
